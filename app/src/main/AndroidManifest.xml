<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.godutch">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:allowBackup="false"
            android:icon="@mipmap/godutch_logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/godutch_logo"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".views.AddCreditCardActivity">
        </activity>
        <activity android:name=".views.CreditCardListActivity">
        </activity>
        <activity android:name=".views.WaiterTableActivity">
        </activity>
        <activity android:name=".views.TableListActivity">
        </activity>
        <activity android:name=".views.TableActivity">
        </activity>
        <activity android:name=".views.SettingsActivity">
        </activity>
        <activity android:name=".views.RestaurantsActivity">
        </activity>
        <activity
                android:name=".views.RegisterActivity"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".views.LoginActivity"
                android:theme="@style/AppTheme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>